/** Concurrent workers for full-document PDF page scans (Extract + Validate). */
export const PDF_PAGE_CONCURRENCY = 6;

/** DPI used when rendering PDF pages to PNG via GraphicsMagick. */
export const PDF_RENDER_DENSITY = 300;
