# Figure frequency and simplified extraction

**Goal:** Count keyword frequency per figure (illustration), not per callout. Show one occurrence row per figure with comma-separated callouts.

**Extraction:** Simplify Gemini instructions — pair callout labels with part names; one text lookup per grouped callouts; indirect references acceptable when no direct label exists.

**Status:** IMPLEMENTED
