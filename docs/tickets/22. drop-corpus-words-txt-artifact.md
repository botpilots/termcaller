# Drop corpus words.txt artifact

**Goal:** Stop generating and tracking `backend/data/service-manual-corpus/words.txt`; runtime only needs `word-rank.json`.

**Why:** `words.txt` is a ~2.7 MB space-separated token dump produced by the build script. Priority lookup uses the compressed `word-rank.json` (~200 KB) exclusively via `corpusLookup.ts`.

**STATUS: IMPLEMENTED**
