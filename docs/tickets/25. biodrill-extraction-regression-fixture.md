# BioDrill500 extraction regression fixture

**Goal:** Capture thinking-era extraction results and compare re-runs (no thinking) on the same PDF.

**Approach:**
1. Export DB snapshot for project `884957d3-a676-4d49-8f2f-6be814878d03` to `test_data/fixtures/biodrill500-thinking-extraction.json`.
2. `collectExtractionFromPdf()` mirrors processing logic in memory for tests.
3. Opt-in vitest compares unique keyword count (primary) and keyword set (secondary).

**Status:** IMPLEMENTED
