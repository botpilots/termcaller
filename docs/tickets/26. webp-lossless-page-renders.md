# WebP lossless page renders for Gemini extraction

**Goal:** Smaller API payloads at 300 DPI while keeping lossless line art quality.

**Approach:** Render PDF pages as WebP lossless via GraphicsMagick; send `image/webp` to Gemini. Re-run LOW thinking semantic regression vs thinking baseline.

**Status:** IMPLEMENTED
