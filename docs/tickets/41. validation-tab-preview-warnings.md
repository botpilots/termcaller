# Validation tab, preview, and warning UX

**Goal:** Rename the Figures browse tab to Validation, use LOW Gemini thinking for validation, align anomaly warnings with extraction's clickable amber-card UX, and add document preview with click-to-highlight for validation anomalies.

**Status:** IMPLEMENTED

---

## Scope

1. **Tab label:** Visible "Validation" label (internal `figures` tab id unchanged); `ShieldCheck` icon.
2. **Backend:** `GEMINI_VALIDATION_THINKING_LEVEL = LOW` on validation chat; callout fallback in `pdfTextLocate` when term search misses.
3. **Frontend:** Refactor `ValidationAnomalies` to flat clickable warning cards; new `FigureDocumentView` with `DocumentPreview`; sidebar Warning badge on figures with anomalies.

---

## Test criteria

- [x] Tab shows Validation; section header says Validation
- [x] Validate runs with LOW thinking (no regression in result shape)
- [x] Anomaly warnings are clickable amber cards; sidebar shows dot + Warning
- [x] Click warning or anomaly row → preview highlights when text layer matches
- [x] Per-figure and batch errors still show red UI
- [x] Callout-only locate fallback tested when term is empty
