# Extraction part-type naming, casing, and Priority scoring

**Goal:** Extract generic part types (not callout disambiguators), preserve manual casing, and weight figure frequency more in Priority sort.

**Changes:**
1. Gemini prompt: part-type only (`terminal` not `terminal (b)`), preserve casing (`O-ring`, `R needle`).
2. Split `canonicalSourceTerm` (display) vs `sourceTermLookupKey` (dedup/corpus); case-insensitive keyword merge.
3. `PRIORITY_FIGURE_EXPONENT`: 0.75 → 1.0.

**STATUS: IMPLEMENTED**
