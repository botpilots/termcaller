# PDF preprocessing timing tests

**Goal:** Measure pre-Gemini PDF preprocessing cost (pdf.js text + ImageMagick render) with opt-in vitest benchmarks.

**Approach:**
1. `RUN_TIMING_TESTS=1` gate to avoid slowing default CI runs.
2. Single-page breakdown: session open, pdf.js text-only, full `extractPageData`.
3. Full-document `scanPdfPages` at production concurrency with per-page and summary stats.
4. Optional Planmed Sophie manual when present in `service_manuals/`.

**Status:** IMPLEMENTED
