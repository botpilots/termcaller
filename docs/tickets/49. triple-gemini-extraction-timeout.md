# Triple Gemini Extraction Timeout

**Goal:** Reduce silent page failures on Cloud Run where dense pages hit the 60s Gemini budget under GCSFuse + concurrent load.

**Change:** `GEMINI_EXTRACTION_TIMEOUT_MS` 60_000 → 180_000 (3×). Applies to both initial extraction and adjacent-page follow-up round-trips.

**Status:** IMPLEMENTED
